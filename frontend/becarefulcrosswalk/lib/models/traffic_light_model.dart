import 'package:flutter/cupertino.dart';

class TrafficLightModel extends StatelessWidget {
  const TrafficLightModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
